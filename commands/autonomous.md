---
disable-model-invocation: true
name: autonomous
description: Bind autonomy mandate — execute phased work end-to-end, one commit per phase, ≤6-line summary. Stops only for prod-destructive ops, spec ambiguity, or repeated test failures.
---

Read the file `~/.claude/skills/my/autonomous/SKILL.md` and follow it exactly (resolve its relative paths against that skill directory). Pass `$ARGUMENTS` as the input.
