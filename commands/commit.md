---
name: commit
description: Commit ONLY the current task's coherent change-set (staged, or the dominant feature bundle) as Conventional Commits — leaves unrelated changes untouched. Local-only.
argument-hint: ""
---

Read the file `~/.claude/skills/git/commit/SKILL.md` and follow it exactly (resolve its relative paths against that skill directory). Pass `$ARGUMENTS` as the input.
