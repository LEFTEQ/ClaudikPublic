---
disable-model-invocation: true
name: research
description: Research-first answer mode — triangulates Tier-1 docs, secondaries, project code, and a contrarian pass via 4 parallel subagents before answering.
---

Read the file `~/.claude/skills/my/triangulated-research/SKILL.md` and follow it exactly (resolve its relative `references/` paths against that skill directory). Pass `$ARGUMENTS` as the input.
