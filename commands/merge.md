---
name: merge
description: Finish the feature — drive the git commands until the current PR is mergeable, merge it (merge commit), remove the worktree + branch, then pull the main clone (never switch it). Fully autonomous.
argument-hint: "[PR] [--admin]"
---

Read the file `~/.claude/skills/git/merge/SKILL.md` and follow it exactly (resolve its relative paths against that skill directory). Pass `$ARGUMENTS` as the input.
