---
name: commit-all
description: Bundle the ENTIRE working tree (incl. other sessions' work) into local Conventional Commits. Skips + reports secrets/artifacts. No gate. Local-only.
argument-hint: ""
---

Read the file `~/.claude/skills/git/commit-all/SKILL.md` and follow it exactly (resolve its relative paths against that skill directory). Pass `$ARGUMENTS` as the input.
