---
disable-model-invocation: true
name: memory
description: Memory maintenance — /memory [save|prune|audit|migrate]. Capture facts now, prune/audit the two memory homes, or migrate a legacy layout.
---

Read `~/.claude/skills/my/memory/SKILL.md` and follow it exactly. Pass `$ARGUMENTS` as the subcommand + input (default subcommand when empty: `save`).
